import { SvgIconsDirective } from './svg-icons.directive';

describe('SvgIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgIconsDirective();
    expect(directive).toBeTruthy();
  });
});
